

\section{Basics}

\begin{frame}{Qu'est-ce que \mongodb/,?}

%\frametitle{Qu'est-ce que \mongodb/,?}

\mongodb/, un système "NoSQL", l'un des plus populaires

\begin{redbullet}
  \item s'appuie sur un modèle de données semi-structuré (encodage JSON)\,;
  \item pas de schéma (complète flexibilité)\,;
  \item un \alert{langage d'interrogation} original, limité (et spécifique)\,;
   \end{redbullet}
   \vfill
   
Construit dès l'origine comme un système \alert{scalable} et \alert{distribué}.

\begin{redbullet}
 \item distribution par partionnement (\textit{sharding})\,;
  \item tolérance aux pannes par réplication.
\end{redbullet}   


\vfill

Un aperçu du \alert{langage d'interrogation} de \mongodb/.


\end{frame}


\begin{frame}
\frametitle{Découvrons (avec le client \texttt{Compass})}

Récupérez Compass et connectez-vous à MongoDB Atlas
\vfill

Créez une base \texttt{Pathologies} et insérez les trois collections
\texttt{region}, \texttt{patients} et \texttt{cas} à partir de nos
fichiers JSON.

\vfill 

Faites quelques essais avec Compass

\vfill

\begin{redbullet}
  \item Insérez un document
  \item Lancez un \texttt{find} avec une requête vide
  \item Accédez au \texttt{shell} Mongo
\end{redbullet}

\vfill

\begin{noteblock}
Vous pouvez récupérer les jeux
de données sur le site.
\end{noteblock}
\end{frame}




\begin{frame}[fragile]
\frametitle{Aperçu du langage de requêtes}

L'équivalent du \texttt{select-from-where}, \alert{sur une
seule collection} (pas de \alert{jointure}).

\vfill

Principe de \textit{pattern matching}\,: on apparie
les documents à la requête.

\vfill

\begin{redbullet}
\item 
\begin{smallverbatim}
find ({"_id": "10"})
\end{smallverbatim}

\item
\begin{smallverbatim}
find ({"nom": "Bretagne"})
\end{smallverbatim}
\item
On peut accéder aux objets imbriqués


\begin{smallverbatim}
find ({"depts.nom": "Finistère"} )
\end{smallverbatim}
\item

\begin{smallverbatim}
find ({"depts.nom": "Finistère"}, , {"nom": 1} )
\end{smallverbatim}
\item
Avec une expression régulière (pas de guillemets!)\,:


\begin{smallverbatim}
find({"nom":  /Nor/})
\end{smallverbatim}

\end{redbullet}
\end{frame}



\begin{frame}[fragile]
\frametitle{Les requêtes, suite}



Par intervalle:

\begin{smallverbatim}
find({"depts.code": {$gte: "50", $lte: "52"}})
\end{smallverbatim}


Quelques clauses ensemblistes 

\begin{verbatim}
{ "nom": { $in: ['Bretagne', 'Normandie', 'Mayotte' ] } }
\end{verbatim}

Autres possibilités\,: \texttt{\$nin}, \texttt{\$all}, \texttt{\$exist}

\begin{verbatim}
{ "nom": { $nin: ['Bretagne', 'Normandie', 'Mayotte' ] } }
\end{verbatim}

\vfill

\begin{noteblock}
Les agrégations existent, mais c'est très compliqué
\end{noteblock}
\end{frame}


\begin{frame}[fragile]
\frametitle{Et les jointures}

Pas de jointure avec Mongo.
Il faut les effectuer du \alert{côté client}. 

\vfill

\figSlideWithSize{jointure-serveur-client}{10cm}

\vfill

En règle générale, il faut programme côté applicatif tout ce qu'on a perdu de SQL
\end{frame}


\begin{frame}[fragile]
\frametitle{À vous de jouer}

Reproduisez les requêtes précédentes sur la collection des régions.

\vfill 
Sur la collection des cas
\begin{redbullet}
  \item Tous les cas d'embolie pulmonaire
\end{redbullet}
\vfill


Sur la collection des patients

\begin{redbullet}
  \item Les pathologies des patients aaron, adam et 'adeline'
\end{redbullet}
\vfill

\end{frame}

